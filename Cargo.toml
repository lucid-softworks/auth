[package]
name = "lucid-auth"
version = "0.1.0"
edition = "2024"
rust-version = "1.90"
description = "Native Rust backend for Better Auth clients"
readme = "README.md"
license = "MIT"
repository = "https://github.com/lucid-softworks/auth"
keywords = ["authentication", "better-auth", "axum", "passkey"]
categories = ["authentication", "web-programming"]

[features]
default = ["axum"]
axum = ["dep:axum"]
postgres = ["dep:sqlx"]

[dependencies]
argon2 = "0.5"
async-trait = "0.1"
axum = { version = "0.8", optional = true }
base64 = "0.22"
chrono = { version = "0.4", features = ["serde"] }
hex = "0.4"
hmac = "0.12"
rand = "0.10"
rand_core = { version = "0.6", features = ["getrandom"] }
reqwest = { version = "0.13", default-features = false, features = ["rustls"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha1 = "0.10"
sha2 = "0.10"
sqlx = { version = "0.8", optional = true, features = ["runtime-tokio-rustls", "postgres", "uuid", "chrono"] }
thiserror = "2"
tokio = { version = "1", features = ["rt", "sync"] }
uuid = { version = "1", features = ["v4", "serde"] }
url = "2"
webauthn-rs = "0.5.5"

[dev-dependencies]
axum = "0.8"
http-body-util = "0.1"
serde_json = "1"
tower = { version = "0.5", features = ["util"] }

[[test]]
name = "access_contract"
required-features = ["axum"]

[[test]]
name = "admin_lifecycle_contract"
required-features = ["axum"]

[[test]]
name = "better_auth_contract"
required-features = ["axum"]

[[test]]
name = "postgres_contract"
required-features = ["postgres"]

[[test]]
name = "request_security_contract"
required-features = ["axum"]
